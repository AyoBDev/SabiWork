# Plan 6: Seed Data Script

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a comprehensive seed script that populates the database with 3 months of simulated economic activity — 20 workers across trust tiers, 10 buyers, 5 traders with sales history, 5 seekers, 50 completed jobs, 200 sales logs, 500 demand signals, trust events, apprenticeships, and field agents. The seed data creates intentional patterns visible in the demo (Surulere tiling gap, heat map clusters, SabiScore progression stories).

**Architecture:** Single `seed.js` script using Knex. Runs after migrations. Uses helper functions to generate realistic Lagos coordinates, timestamps spread over 90 days, and correlated data (jobs generate trust events, sales logs affect SabiScore). JSON fixture files for readability, script handles insertion order and relationship wiring.

**Tech Stack:** Node.js, Knex, shared/constants.js

**Depends on:** Plan 1 (database migrations must exist)

---

## File Structure

```
backend/
├── seed.js                    # Main seed runner (called by docker-compose)
└── seed-data/
    ├── helpers.js             # Date generation, coordinate helpers
    ├── workers.json           # 20 workers with GPS, trades, trust tiers
    ├── buyers.json            # 10 buyers
    ├── traders.json           # 5 traders with business types
    ├── seekers.json           # 5 seekers
    ├── agents.json            # 2 field agents
    └── apprenticeships.json   # 3 apprenticeships
```

---

### Task 1: Seed Helpers

**Files:**
- Create: `backend/seed-data/helpers.js`

- [ ] **Step 1: Create seed helpers module**

```javascript
// backend/seed-data/helpers.js

/**
 * Lagos area center coordinates for realistic GPS spread
 */
const AREA_CENTERS = {
  surulere: { lat: 6.5010, lng: 3.3569 },
  yaba: { lat: 6.5095, lng: 3.3711 },
  ikeja: { lat: 6.6018, lng: 3.3515 },
  lekki: { lat: 6.4478, lng: 3.4723 },
  victoria_island: { lat: 6.4281, lng: 3.4219 },
  mushin: { lat: 6.5355, lng: 3.3554 },
  maryland: { lat: 6.5667, lng: 3.3636 },
  ojota: { lat: 6.5833, lng: 3.3833 },
  ikorodu: { lat: 6.6194, lng: 3.5105 },
  ajah: { lat: 6.4698, lng: 3.5852 }
};

/**
 * Jitter a coordinate by up to ~200m for realistic clustering
 */
function jitterCoord(lat, lng, maxMeters = 200) {
  const latJitter = (Math.random() - 0.5) * (maxMeters / 111000) * 2;
  const lngJitter = (Math.random() - 0.5) * (maxMeters / 111000) * 2;
  return {
    lat: parseFloat((lat + latJitter).toFixed(6)),
    lng: parseFloat((lng + lngJitter).toFixed(6))
  };
}

/**
 * Generate a random date within the last N days
 */
function randomDateWithinDays(days) {
  const now = Date.now();
  const offset = Math.random() * days * 24 * 60 * 60 * 1000;
  return new Date(now - offset);
}

/**
 * Generate a date within a specific range (days ago)
 */
function dateBetweenDaysAgo(startDaysAgo, endDaysAgo) {
  const now = Date.now();
  const start = now - startDaysAgo * 24 * 60 * 60 * 1000;
  const end = now - endDaysAgo * 24 * 60 * 60 * 1000;
  return new Date(start + Math.random() * (end - start));
}

/**
 * Pick random items from array
 */
function pickRandom(arr, count = 1) {
  const shuffled = [...arr].sort(() => 0.5 - Math.random());
  return count === 1 ? shuffled[0] : shuffled.slice(0, count);
}

/**
 * Generate a Nigerian phone number
 */
function generatePhone(index) {
  const prefixes = ['0803', '0805', '0807', '0810', '0813', '0816', '0903', '0906'];
  const prefix = prefixes[index % prefixes.length];
  const suffix = String(1000000 + index * 7919).slice(-7); // deterministic but varied
  return `${prefix}${suffix}`;
}

/**
 * Generate a Squad-style transaction reference
 */
function generateTransRef(jobId, timestamp) {
  return `SABI_${jobId}_${timestamp.getTime()}`;
}

module.exports = {
  AREA_CENTERS,
  jitterCoord,
  randomDateWithinDays,
  dateBetweenDaysAgo,
  pickRandom,
  generatePhone,
  generateTransRef
};
```

- [ ] **Step 2: Verify the file runs without errors**

Run: `cd backend && node -e "const h = require('./seed-data/helpers'); console.log(h.jitterCoord(6.5, 3.35)); console.log(h.generatePhone(1));"`

Expected: Prints a coordinate object and a phone number string without errors.

- [ ] **Step 3: Commit**

```bash
git add backend/seed-data/helpers.js
git commit -m "feat: add seed data helpers (coordinates, dates, phone gen)"
```

---

### Task 2: Workers Fixture (20 Workers)

**Files:**
- Create: `backend/seed-data/workers.json`

- [ ] **Step 1: Create workers fixture**

```json
[
  {
    "name": "Emeka Okafor",
    "phone": "08031000001",
    "primary_trade": "plumbing",
    "secondary_trades": ["tiling"],
    "service_areas": ["surulere", "yaba", "mushin"],
    "area_key": "surulere",
    "bank_code": "058",
    "account_number": "0123456701",
    "account_name": "EMEKA OKAFOR",
    "virtual_account_number": "9900000001",
    "trust_score": 0.76,
    "sabi_score": 62,
    "total_jobs": 34,
    "total_income": 510000,
    "accepts_apprentices": true,
    "apprentice_slots": 2,
    "is_available": true,
    "onboarding_channel": "whatsapp",
    "gps_verified": true
  },
  {
    "name": "Aisha Mohammed",
    "phone": "08051000002",
    "primary_trade": "tailoring",
    "secondary_trades": ["fashion_design"],
    "service_areas": ["ikeja", "maryland", "ojota"],
    "area_key": "ikeja",
    "bank_code": "033",
    "account_number": "0123456702",
    "account_name": "AISHA MOHAMMED",
    "virtual_account_number": "9900000002",
    "trust_score": 0.83,
    "sabi_score": 71,
    "total_jobs": 45,
    "total_income": 675000,
    "accepts_apprentices": true,
    "apprentice_slots": 3,
    "is_available": true,
    "onboarding_channel": "field_agent",
    "gps_verified": true
  },
  {
    "name": "Chinedu Eze",
    "phone": "08071000003",
    "primary_trade": "electrical",
    "secondary_trades": [],
    "service_areas": ["lekki", "victoria_island", "ajah"],
    "area_key": "lekki",
    "bank_code": "044",
    "account_number": "0123456703",
    "account_name": "CHINEDU EZE",
    "virtual_account_number": "9900000003",
    "trust_score": 0.91,
    "sabi_score": 78,
    "total_jobs": 58,
    "total_income": 1160000,
    "accepts_apprentices": true,
    "apprentice_slots": 1,
    "is_available": true,
    "onboarding_channel": "whatsapp",
    "gps_verified": true
  },
  {
    "name": "Folake Adeyemi",
    "phone": "08101000004",
    "primary_trade": "hairdressing",
    "secondary_trades": ["makeup"],
    "service_areas": ["yaba", "surulere", "maryland"],
    "area_key": "yaba",
    "bank_code": "058",
    "account_number": "0123456704",
    "account_name": "FOLAKE ADEYEMI",
    "virtual_account_number": "9900000004",
    "trust_score": 0.54,
    "sabi_score": 45,
    "total_jobs": 18,
    "total_income": 216000,
    "accepts_apprentices": false,
    "apprentice_slots": 0,
    "is_available": true,
    "onboarding_channel": "whatsapp",
    "gps_verified": false
  },
  {
    "name": "Tunde Bakare",
    "phone": "08131000005",
    "primary_trade": "carpentry",
    "secondary_trades": ["furniture"],
    "service_areas": ["mushin", "surulere", "yaba"],
    "area_key": "mushin",
    "bank_code": "011",
    "account_number": "0123456705",
    "account_name": "TUNDE BAKARE",
    "virtual_account_number": "9900000005",
    "trust_score": 0.38,
    "sabi_score": 33,
    "total_jobs": 9,
    "total_income": 108000,
    "accepts_apprentices": false,
    "apprentice_slots": 0,
    "is_available": true,
    "onboarding_channel": "ussd",
    "gps_verified": false
  },
  {
    "name": "Blessing Nwosu",
    "phone": "08161000006",
    "primary_trade": "cleaning",
    "secondary_trades": ["laundry"],
    "service_areas": ["lekki", "victoria_island"],
    "area_key": "victoria_island",
    "bank_code": "058",
    "account_number": "0123456706",
    "account_name": "BLESSING NWOSU",
    "virtual_account_number": "9900000006",
    "trust_score": 0.67,
    "sabi_score": 55,
    "total_jobs": 28,
    "total_income": 280000,
    "accepts_apprentices": false,
    "apprentice_slots": 0,
    "is_available": true,
    "onboarding_channel": "whatsapp",
    "gps_verified": true
  },
  {
    "name": "Ibrahim Yusuf",
    "phone": "09031000007",
    "primary_trade": "painting",
    "secondary_trades": ["screeding"],
    "service_areas": ["ikeja", "ojota", "maryland"],
    "area_key": "ojota",
    "bank_code": "033",
    "account_number": "0123456707",
    "account_name": "IBRAHIM YUSUF",
    "virtual_account_number": "9900000007",
    "trust_score": 0.22,
    "sabi_score": 18,
    "total_jobs": 4,
    "total_income": 48000,
    "accepts_apprentices": false,
    "apprentice_slots": 0,
    "is_available": true,
    "onboarding_channel": "field_agent",
    "gps_verified": true
  },
  {
    "name": "Ngozi Obi",
    "phone": "09061000008",
    "primary_trade": "catering",
    "secondary_trades": ["baking"],
    "service_areas": ["surulere", "yaba", "mushin"],
    "area_key": "surulere",
    "bank_code": "044",
    "account_number": "0123456708",
    "account_name": "NGOZI OBI",
    "virtual_account_number": "9900000008",
    "trust_score": 0.61,
    "sabi_score": 51,
    "total_jobs": 22,
    "total_income": 330000,
    "accepts_apprentices": true,
    "apprentice_slots": 2,
    "is_available": false,
    "onboarding_channel": "whatsapp",
    "gps_verified": true
  },
  {
    "name": "Oluwaseun Ajayi",
    "phone": "08031000009",
    "primary_trade": "welding",
    "secondary_trades": ["metal_fabrication"],
    "service_areas": ["ikorodu", "ojota"],
    "area_key": "ikorodu",
    "bank_code": "011",
    "account_number": "0123456709",
    "account_name": "OLUWASEUN AJAYI",
    "virtual_account_number": "9900000009",
    "trust_score": 0.45,
    "sabi_score": 38,
    "total_jobs": 12,
    "total_income": 180000,
    "accepts_apprentices": false,
    "apprentice_slots": 0,
    "is_available": true,
    "onboarding_channel": "ussd",
    "gps_verified": false
  },
  {
    "name": "Fatima Bello",
    "phone": "08051000010",
    "primary_trade": "tailoring",
    "secondary_trades": [],
    "service_areas": ["mushin", "surulere"],
    "area_key": "mushin",
    "bank_code": "058",
    "account_number": "0123456710",
    "account_name": "FATIMA BELLO",
    "virtual_account_number": "9900000010",
    "trust_score": 0.31,
    "sabi_score": 27,
    "total_jobs": 7,
    "total_income": 84000,
    "accepts_apprentices": false,
    "apprentice_slots": 0,
    "is_available": true,
    "onboarding_channel": "whatsapp",
    "gps_verified": false
  },
  {
    "name": "Kola Ogundimu",
    "phone": "08071000011",
    "primary_trade": "plumbing",
    "secondary_trades": ["tiling"],
    "service_areas": ["ikeja", "maryland", "ojota"],
    "area_key": "maryland",
    "bank_code": "033",
    "account_number": "0123456711",
    "account_name": "KOLA OGUNDIMU",
    "virtual_account_number": "9900000011",
    "trust_score": 0.69,
    "sabi_score": 57,
    "total_jobs": 26,
    "total_income": 390000,
    "accepts_apprentices": true,
    "apprentice_slots": 1,
    "is_available": true,
    "onboarding_channel": "field_agent",
    "gps_verified": true
  },
  {
    "name": "Adebayo Oladipo",
    "phone": "08101000012",
    "primary_trade": "electrical",
    "secondary_trades": ["electrical"],
    "service_areas": ["surulere", "mushin", "yaba"],
    "area_key": "surulere",
    "bank_code": "044",
    "account_number": "0123456712",
    "account_name": "ADEBAYO OLADIPO",
    "virtual_account_number": "9900000012",
    "trust_score": 0.52,
    "sabi_score": 42,
    "total_jobs": 15,
    "total_income": 225000,
    "accepts_apprentices": false,
    "apprentice_slots": 0,
    "is_available": true,
    "onboarding_channel": "whatsapp",
    "gps_verified": true
  },
  {
    "name": "Mercy Okonkwo",
    "phone": "08131000013",
    "primary_trade": "cleaning",
    "secondary_trades": ["fumigation"],
    "service_areas": ["ikeja", "lekki", "victoria_island"],
    "area_key": "ikeja",
    "bank_code": "011",
    "account_number": "0123456713",
    "account_name": "MERCY OKONKWO",
    "virtual_account_number": "9900000013",
    "trust_score": 0.85,
    "sabi_score": 73,
    "total_jobs": 52,
    "total_income": 520000,
    "accepts_apprentices": false,
    "apprentice_slots": 0,
    "is_available": true,
    "onboarding_channel": "whatsapp",
    "gps_verified": true
  },
  {
    "name": "Uche Nnamdi",
    "phone": "08161000014",
    "primary_trade": "carpentry",
    "secondary_trades": ["roofing"],
    "service_areas": ["ajah", "lekki"],
    "area_key": "ajah",
    "bank_code": "058",
    "account_number": "0123456714",
    "account_name": "UCHE NNAMDI",
    "virtual_account_number": "9900000014",
    "trust_score": 0.73,
    "sabi_score": 60,
    "total_jobs": 30,
    "total_income": 450000,
    "accepts_apprentices": true,
    "apprentice_slots": 2,
    "is_available": false,
    "onboarding_channel": "field_agent",
    "gps_verified": true
  },
  {
    "name": "Sade Olumide",
    "phone": "09031000015",
    "primary_trade": "hairdressing",
    "secondary_trades": ["nails", "lashes"],
    "service_areas": ["lekki", "victoria_island", "ajah"],
    "area_key": "lekki",
    "bank_code": "033",
    "account_number": "0123456715",
    "account_name": "SADE OLUMIDE",
    "virtual_account_number": "9900000015",
    "trust_score": 0.88,
    "sabi_score": 75,
    "total_jobs": 60,
    "total_income": 900000,
    "accepts_apprentices": true,
    "apprentice_slots": 3,
    "is_available": true,
    "onboarding_channel": "whatsapp",
    "gps_verified": true
  },
  {
    "name": "Musa Abdullahi",
    "phone": "09061000016",
    "primary_trade": "painting",
    "secondary_trades": ["POP_ceiling"],
    "service_areas": ["ikorodu", "ojota", "maryland"],
    "area_key": "ikorodu",
    "bank_code": "044",
    "account_number": "0123456716",
    "account_name": "MUSA ABDULLAHI",
    "virtual_account_number": "9900000016",
    "trust_score": 0.41,
    "sabi_score": 35,
    "total_jobs": 10,
    "total_income": 150000,
    "accepts_apprentices": false,
    "apprentice_slots": 0,
    "is_available": true,
    "onboarding_channel": "ussd",
    "gps_verified": false
  },
  {
    "name": "Grace Adekunle",
    "phone": "08031000017",
    "primary_trade": "catering",
    "secondary_trades": ["event_decor"],
    "service_areas": ["yaba", "surulere", "ikeja"],
    "area_key": "yaba",
    "bank_code": "011",
    "account_number": "0123456717",
    "account_name": "GRACE ADEKUNLE",
    "virtual_account_number": "9900000017",
    "trust_score": 0.58,
    "sabi_score": 48,
    "total_jobs": 20,
    "total_income": 400000,
    "accepts_apprentices": false,
    "apprentice_slots": 0,
    "is_available": true,
    "onboarding_channel": "whatsapp",
    "gps_verified": true
  },
  {
    "name": "Obinna Chukwu",
    "phone": "08051000018",
    "primary_trade": "welding",
    "secondary_trades": ["aluminium"],
    "service_areas": ["mushin", "surulere", "yaba"],
    "area_key": "mushin",
    "bank_code": "058",
    "account_number": "0123456718",
    "account_name": "OBINNA CHUKWU",
    "virtual_account_number": "9900000018",
    "trust_score": 0.15,
    "sabi_score": 12,
    "total_jobs": 2,
    "total_income": 24000,
    "accepts_apprentices": false,
    "apprentice_slots": 0,
    "is_available": true,
    "onboarding_channel": "whatsapp",
    "gps_verified": false
  },
  {
    "name": "Funke Abiodun",
    "phone": "08071000019",
    "primary_trade": "tailoring",
    "secondary_trades": ["adire"],
    "service_areas": ["ajah", "lekki"],
    "area_key": "ajah",
    "bank_code": "033",
    "account_number": "0123456719",
    "account_name": "FUNKE ABIODUN",
    "virtual_account_number": "9900000019",
    "trust_score": 0.63,
    "sabi_score": 52,
    "total_jobs": 24,
    "total_income": 288000,
    "accepts_apprentices": true,
    "apprentice_slots": 1,
    "is_available": true,
    "onboarding_channel": "field_agent",
    "gps_verified": true
  },
  {
    "name": "Yemi Olatunde",
    "phone": "08101000020",
    "primary_trade": "tiling",
    "secondary_trades": ["screeding"],
    "service_areas": ["ikeja", "maryland"],
    "area_key": "ikeja",
    "bank_code": "044",
    "account_number": "0123456720",
    "account_name": "YEMI OLATUNDE",
    "virtual_account_number": "9900000020",
    "trust_score": 0.48,
    "sabi_score": 40,
    "total_jobs": 13,
    "total_income": 195000,
    "accepts_apprentices": false,
    "apprentice_slots": 0,
    "is_available": true,
    "onboarding_channel": "whatsapp",
    "gps_verified": true
  }
]
```

**Key design decisions:**
- NO tilers in Surulere (intentional gap for demo)
- Emeka (worker 1): trust 0.76, good for buyer matching demo
- Trust tiers represented: Emerging (0.15, 0.22), Trusted (0.31-0.58), Verified (0.61-0.79), Elite (0.83-0.91)
- Mix of onboarding channels: whatsapp (12), field_agent (5), ussd (3)
- Mix of GPS verified (14) and not (6)
- Trades distributed: plumbing(2), tailoring(3), electrical(2), hairdressing(2), carpentry(2), cleaning(2), painting(2), catering(2), welding(2), tiling(1-in ikeja only)

- [ ] **Step 2: Verify JSON is valid**

Run: `cd backend && node -e "const w = require('./seed-data/workers.json'); console.log(w.length + ' workers loaded');"`

Expected: `20 workers loaded`

- [ ] **Step 3: Commit**

```bash
git add backend/seed-data/workers.json
git commit -m "feat: add 20 workers fixture with trust tiers and Lagos GPS"
```

---

### Task 3: Buyers, Traders, Seekers, Agents Fixtures

**Files:**
- Create: `backend/seed-data/buyers.json`
- Create: `backend/seed-data/traders.json`
- Create: `backend/seed-data/seekers.json`
- Create: `backend/seed-data/agents.json`
- Create: `backend/seed-data/apprenticeships.json`

- [ ] **Step 1: Create buyers fixture**

```json
[
  { "name": "Chioma Okafor", "phone": "08031200001", "email": "chioma@gmail.com", "area": "surulere", "area_key": "surulere" },
  { "name": "Adamu Hassan", "phone": "08051200002", "email": "adamu.h@yahoo.com", "area": "ikeja", "area_key": "ikeja" },
  { "name": "Ngozi Ibe", "phone": "08071200003", "email": "ngozi.ibe@gmail.com", "area": "lekki", "area_key": "lekki" },
  { "name": "Tope Williams", "phone": "08101200004", "email": "tope.w@gmail.com", "area": "yaba", "area_key": "yaba" },
  { "name": "Bola Ogunleye", "phone": "08131200005", "email": "bola.o@hotmail.com", "area": "victoria_island", "area_key": "victoria_island" },
  { "name": "Ahmed Suleiman", "phone": "08161200006", "email": "ahmed.s@gmail.com", "area": "mushin", "area_key": "mushin" },
  { "name": "Funmi Ojo", "phone": "09031200007", "email": "funmi.ojo@gmail.com", "area": "maryland", "area_key": "maryland" },
  { "name": "Emeka Ugwu", "phone": "09061200008", "email": "emeka.u@yahoo.com", "area": "ojota", "area_key": "ojota" },
  { "name": "Kemi Adewale", "phone": "08031200009", "email": "kemi.a@gmail.com", "area": "surulere", "area_key": "surulere" },
  { "name": "Yusuf Balogun", "phone": "08051200010", "email": "yusuf.b@gmail.com", "area": "ikorodu", "area_key": "ikorodu" }
]
```

- [ ] **Step 2: Create traders fixture**

```json
[
  {
    "name": "Mama Ngozi",
    "phone": "08031300001",
    "business_type": "food_provisions",
    "area": "surulere",
    "area_key": "surulere",
    "virtual_account_number": "9900100001",
    "sabi_score": 43,
    "total_logged_sales": 156,
    "total_logged_revenue": 2340000
  },
  {
    "name": "Alhaji Musa",
    "phone": "08051300002",
    "business_type": "building_materials",
    "area": "mushin",
    "area_key": "mushin",
    "virtual_account_number": "9900100002",
    "sabi_score": 58,
    "total_logged_sales": 89,
    "total_logged_revenue": 4450000
  },
  {
    "name": "Mrs. Balogun",
    "phone": "08071300003",
    "business_type": "textiles",
    "area": "yaba",
    "area_key": "yaba",
    "virtual_account_number": "9900100003",
    "sabi_score": 35,
    "total_logged_sales": 67,
    "total_logged_revenue": 1340000
  },
  {
    "name": "Chief Okoro",
    "phone": "08101300004",
    "business_type": "electronics",
    "area": "ikeja",
    "area_key": "ikeja",
    "virtual_account_number": "9900100004",
    "sabi_score": 67,
    "total_logged_sales": 203,
    "total_logged_revenue": 8120000
  },
  {
    "name": "Iya Basira",
    "phone": "08131300005",
    "business_type": "food_provisions",
    "area": "ojota",
    "area_key": "ojota",
    "virtual_account_number": "9900100005",
    "sabi_score": 28,
    "total_logged_sales": 45,
    "total_logged_revenue": 675000
  }
]
```

**Key:** Mama Ngozi has SabiScore 43 — "7 weeks to loan" story for demo.

- [ ] **Step 3: Create seekers fixture**

```json
[
  { "name": "David Okechukwu", "phone": "08031400001", "area": "surulere", "area_key": "surulere", "interests": ["tiling", "plumbing"] },
  { "name": "Amina Garba", "phone": "08051400002", "area": "ikeja", "area_key": "ikeja", "interests": ["tailoring", "fashion_design"] },
  { "name": "Samuel Eze", "phone": "08071400003", "area": "yaba", "area_key": "yaba", "interests": ["electrical", "welding"] },
  { "name": "Halima Bello", "phone": "08101400004", "area": "mushin", "area_key": "mushin", "interests": ["hairdressing", "makeup"] },
  { "name": "Chidi Nwosu", "phone": "08131400005", "area": "lekki", "area_key": "lekki", "interests": ["carpentry", "furniture"] }
]
```

- [ ] **Step 4: Create agents fixture**

```json
[
  {
    "name": "Agent Tayo",
    "phone": "08031500001",
    "area_assigned": "surulere",
    "workers_onboarded": 8,
    "is_active": true
  },
  {
    "name": "Agent Fola",
    "phone": "08051500002",
    "area_assigned": "mushin",
    "workers_onboarded": 5,
    "is_active": true
  }
]
```

- [ ] **Step 5: Create apprenticeships fixture**

```json
[
  {
    "master_worker_index": 0,
    "apprentice_name": "David Okechukwu",
    "seeker_index": 0,
    "trade": "plumbing",
    "duration_weeks": 12,
    "weekly_stipend": 5000,
    "status": "active",
    "milestones_completed": 6,
    "total_milestones": 12,
    "started_days_ago": 42
  },
  {
    "master_worker_index": 1,
    "apprentice_name": "Amina Garba",
    "seeker_index": 1,
    "trade": "tailoring",
    "duration_weeks": 16,
    "weekly_stipend": 4000,
    "status": "completed",
    "milestones_completed": 16,
    "total_milestones": 16,
    "started_days_ago": 120
  },
  {
    "master_worker_index": 14,
    "apprentice_name": "Halima Bello",
    "seeker_index": 3,
    "trade": "hairdressing",
    "duration_weeks": 8,
    "weekly_stipend": 3500,
    "status": "active",
    "milestones_completed": 2,
    "total_milestones": 8,
    "started_days_ago": 14
  }
]
```

- [ ] **Step 6: Verify all fixtures**

Run: `cd backend && node -e "console.log('buyers:', require('./seed-data/buyers.json').length); console.log('traders:', require('./seed-data/traders.json').length); console.log('seekers:', require('./seed-data/seekers.json').length); console.log('agents:', require('./seed-data/agents.json').length); console.log('apprenticeships:', require('./seed-data/apprenticeships.json').length);"`

Expected:
```
buyers: 10
traders: 5
seekers: 5
agents: 2
apprenticeships: 3
```

- [ ] **Step 7: Commit**

```bash
git add backend/seed-data/buyers.json backend/seed-data/traders.json backend/seed-data/seekers.json backend/seed-data/agents.json backend/seed-data/apprenticeships.json
git commit -m "feat: add buyers, traders, seekers, agents, apprenticeships fixtures"
```

---

### Task 4: Main Seed Script — Insert Core Entities

**Files:**
- Create: `backend/seed.js`

- [ ] **Step 1: Create the seed script with core entity insertion**

```javascript
// backend/seed.js
const knex = require('./src/database/knex');
const { AREA_CENTERS, jitterCoord, randomDateWithinDays, dateBetweenDaysAgo, pickRandom, generateTransRef } = require('./seed-data/helpers');

const workers = require('./seed-data/workers.json');
const buyers = require('./seed-data/buyers.json');
const traders = require('./seed-data/traders.json');
const seekers = require('./seed-data/seekers.json');
const agents = require('./seed-data/agents.json');
const apprenticeships = require('./seed-data/apprenticeships.json');

async function seed() {
  console.log('🌱 Seeding SabiWork database...');

  // Clear existing data (order matters for foreign keys)
  await knex('apprenticeships').del();
  await knex('trust_events').del();
  await knex('webhook_events').del();
  await knex('demand_signals').del();
  await knex('sales_logs').del();
  await knex('jobs').del();
  await knex('seekers').del();
  await knex('traders').del();
  await knex('buyers').del();
  await knex('agents').del();
  await knex('workers').del();

  console.log('  Cleared existing data');

  // === 1. Insert Workers ===
  const workerIds = [];
  for (const w of workers) {
    const coords = jitterCoord(AREA_CENTERS[w.area_key].lat, AREA_CENTERS[w.area_key].lng);
    const [inserted] = await knex('workers').insert({
      name: w.name,
      phone: w.phone,
      primary_trade: w.primary_trade,
      secondary_trades: JSON.stringify(w.secondary_trades),
      service_areas: JSON.stringify(w.service_areas),
      location_lat: coords.lat,
      location_lng: coords.lng,
      bank_code: w.bank_code,
      account_number: w.account_number,
      account_name: w.account_name,
      virtual_account_number: w.virtual_account_number,
      trust_score: w.trust_score,
      sabi_score: w.sabi_score,
      total_jobs: w.total_jobs,
      total_income: w.total_income,
      accepts_apprentices: w.accepts_apprentices,
      apprentice_slots: w.apprentice_slots,
      is_available: w.is_available,
      onboarding_channel: w.onboarding_channel,
      gps_verified: w.gps_verified,
      last_active_at: randomDateWithinDays(3),
      created_at: dateBetweenDaysAgo(90, 60)
    }).returning('id');
    workerIds.push(inserted.id || inserted);
  }
  console.log(`  ✓ ${workerIds.length} workers inserted`);

  // === 2. Insert Buyers ===
  const buyerIds = [];
  for (const b of buyers) {
    const coords = jitterCoord(AREA_CENTERS[b.area_key].lat, AREA_CENTERS[b.area_key].lng);
    const [inserted] = await knex('buyers').insert({
      name: b.name,
      phone: b.phone,
      email: b.email,
      area: b.area,
      location_lat: coords.lat,
      location_lng: coords.lng,
      created_at: dateBetweenDaysAgo(85, 30)
    }).returning('id');
    buyerIds.push(inserted.id || inserted);
  }
  console.log(`  ✓ ${buyerIds.length} buyers inserted`);

  // === 3. Insert Traders ===
  const traderIds = [];
  for (const t of traders) {
    const coords = jitterCoord(AREA_CENTERS[t.area_key].lat, AREA_CENTERS[t.area_key].lng);
    const [inserted] = await knex('traders').insert({
      name: t.name,
      phone: t.phone,
      business_type: t.business_type,
      area: t.area,
      location_lat: coords.lat,
      location_lng: coords.lng,
      virtual_account_number: t.virtual_account_number,
      sabi_score: t.sabi_score,
      total_logged_sales: t.total_logged_sales,
      total_logged_revenue: t.total_logged_revenue,
      created_at: dateBetweenDaysAgo(90, 60)
    }).returning('id');
    traderIds.push(inserted.id || inserted);
  }
  console.log(`  ✓ ${traderIds.length} traders inserted`);

  // === 4. Insert Seekers ===
  const seekerIds = [];
  for (const s of seekers) {
    const coords = jitterCoord(AREA_CENTERS[s.area_key].lat, AREA_CENTERS[s.area_key].lng);
    const [inserted] = await knex('seekers').insert({
      name: s.name,
      phone: s.phone,
      area: s.area,
      location_lat: coords.lat,
      location_lng: coords.lng,
      interests: JSON.stringify(s.interests),
      created_at: dateBetweenDaysAgo(60, 14)
    }).returning('id');
    seekerIds.push(inserted.id || inserted);
  }
  console.log(`  ✓ ${seekerIds.length} seekers inserted`);

  // === 5. Insert Agents ===
  for (const a of agents) {
    await knex('agents').insert({
      name: a.name,
      phone: a.phone,
      area_assigned: a.area_assigned,
      workers_onboarded: a.workers_onboarded,
      is_active: a.is_active,
      created_at: dateBetweenDaysAgo(90, 80)
    });
  }
  console.log('  ✓ 2 agents inserted');

  // === 6. Generate Jobs (50 completed) ===
  const trades = ['plumbing', 'electrical', 'carpentry', 'cleaning', 'tailoring', 'hairdressing', 'painting', 'catering', 'welding', 'tiling'];
  const jobIds = [];
  const amounts = [5000, 7500, 10000, 12000, 15000, 20000, 25000, 30000, 35000, 50000];
  const channels = ['card', 'transfer', 'ussd', 'bank'];

  for (let i = 0; i < 50; i++) {
    const buyerIdx = i % buyerIds.length;
    const workerIdx = i % workerIds.length;
    const trade = workers[workerIdx].primary_trade;
    const amount = pickRandom(amounts);
    const payout = Math.round(amount * 0.95);
    const createdAt = dateBetweenDaysAgo(85, 2);
    const paidAt = new Date(createdAt.getTime() + 30 * 60 * 1000); // 30 min after creation
    const completedAt = new Date(paidAt.getTime() + (2 + Math.random() * 48) * 60 * 60 * 1000);
    const transRef = generateTransRef(i + 1, paidAt);
    const buyerCoords = jitterCoord(AREA_CENTERS[buyers[buyerIdx].area_key].lat, AREA_CENTERS[buyers[buyerIdx].area_key].lng);

    const [inserted] = await knex('jobs').insert({
      buyer_id: buyerIds[buyerIdx],
      worker_id: workerIds[workerIdx],
      service_category: trade,
      description: `${trade} job #${i + 1}`,
      area: buyers[buyerIdx].area,
      location_lat: buyerCoords.lat,
      location_lng: buyerCoords.lng,
      agreed_amount: amount,
      status: 'completed',
      transaction_ref: transRef,
      payment_channel: pickRandom(channels),
      paid_at: paidAt,
      payout_ref: `PAYOUT_${transRef}`,
      payout_amount: payout,
      payout_status: 'success',
      payout_nip_ref: `NIP${String(100000 + i).slice(-6)}`,
      buyer_rating: Math.min(5, Math.max(3, Math.round(3.5 + Math.random() * 2))),
      created_at: createdAt,
      completed_at: completedAt
    }).returning('id');
    jobIds.push(inserted.id || inserted);
  }
  console.log(`  ✓ ${jobIds.length} completed jobs inserted`);

  // === 7. Generate Trust Events (from jobs) ===
  let trustEventCount = 0;
  for (let i = 0; i < jobIds.length; i++) {
    const workerIdx = i % workerIds.length;
    const workerId = workerIds[workerIdx];
    const jobCreatedAt = dateBetweenDaysAgo(85, 2);

    // Payment received event
    await knex('trust_events').insert({
      worker_id: workerId,
      event_type: 'payment_received',
      score_delta: 0.005,
      score_after: Math.min(1, workers[workerIdx].trust_score),
      job_id: jobIds[i],
      created_at: new Date(jobCreatedAt.getTime() + 35 * 60 * 1000)
    });
    trustEventCount++;

    // Fast payment bonus (50% of jobs)
    if (i % 2 === 0) {
      await knex('trust_events').insert({
        worker_id: workerId,
        event_type: 'fast_payment',
        score_delta: 0.02,
        score_after: Math.min(1, workers[workerIdx].trust_score),
        job_id: jobIds[i],
        created_at: new Date(jobCreatedAt.getTime() + 36 * 60 * 1000)
      });
      trustEventCount++;
    }

    // Rating event (all jobs have ratings)
    const rating = Math.min(5, Math.max(3, Math.round(3.5 + Math.random() * 2)));
    const ratingDelta = rating > 3 ? (rating - 3) * 0.01 : (rating - 3) * 0.02;
    await knex('trust_events').insert({
      worker_id: workerId,
      event_type: 'rating_received',
      score_delta: ratingDelta,
      score_after: Math.min(1, workers[workerIdx].trust_score),
      job_id: jobIds[i],
      created_at: new Date(jobCreatedAt.getTime() + 72 * 60 * 60 * 1000)
    });
    trustEventCount++;
  }
  console.log(`  ✓ ${trustEventCount} trust events inserted`);

  // === 8. Generate Sales Logs (200 for traders) ===
  const foodItems = ['rice', 'beans', 'garri', 'palm oil', 'semovita', 'flour', 'sugar', 'salt', 'maggi', 'tomato paste'];
  const buildingItems = ['cement', 'rod', 'sand', 'gravel', 'blocks', 'nails', 'roofing sheets', 'tiles', 'paint', 'POP'];
  const textileItems = ['ankara', 'lace', 'guinea brocade', 'adire', 'aso-oke', 'plain white', 'chiffon', 'silk', 'cotton', 'velvet'];
  const electronicsItems = ['phone', 'charger', 'earpiece', 'power bank', 'laptop', 'router', 'TV', 'speaker', 'cable', 'adapter'];
  const itemsByType = {
    food_provisions: foodItems,
    building_materials: buildingItems,
    textiles: textileItems,
    electronics: electronicsItems
  };
  const paymentMethods = ['cash', 'transfer', 'squad', 'pos'];

  let salesCount = 0;
  for (let i = 0; i < 200; i++) {
    const traderIdx = i % traderIds.length;
    const trader = traders[traderIdx];
    const items = itemsByType[trader.business_type] || foodItems;
    const item = pickRandom(items);
    const quantity = Math.ceil(Math.random() * 10);
    const basePrice = trader.business_type === 'electronics' ? 15000 + Math.random() * 85000
      : trader.business_type === 'building_materials' ? 5000 + Math.random() * 45000
      : 500 + Math.random() * 15000;
    const amount = Math.round(basePrice * quantity);

    await knex('sales_logs').insert({
      trader_id: traderIds[traderIdx],
      amount: amount,
      item_name: item,
      quantity: quantity,
      category: trader.business_type,
      payment_method: pickRandom(paymentMethods),
      squad_ref: i % 4 === 0 ? `SQUAD_SALE_${i}` : null,
      logged_at: dateBetweenDaysAgo(90, 0)
    });
    salesCount++;
  }
  console.log(`  ✓ ${salesCount} sales logs inserted`);

  // === 9. Generate Demand Signals (500) ===
  // Intentional pattern: heavy tiling demand in Surulere (no tilers there!)
  const demandTrades = ['plumbing', 'electrical', 'carpentry', 'cleaning', 'tailoring', 'hairdressing', 'painting', 'catering', 'welding', 'tiling'];
  const areas = Object.keys(AREA_CENTERS);
  let demandCount = 0;

  for (let i = 0; i < 500; i++) {
    let trade, area;

    // 15% of demand is tiling in Surulere (creates visible gap)
    if (i < 75) {
      trade = 'tiling';
      area = 'surulere';
    }
    // 10% clustered around 3rd Mainland Bridge axis (yaba/surulere/mushin)
    else if (i < 125) {
      trade = pickRandom(demandTrades);
      area = pickRandom(['yaba', 'surulere', 'mushin']);
    }
    // Rest spread across areas and trades
    else {
      trade = pickRandom(demandTrades);
      area = pickRandom(areas);
    }

    const coords = jitterCoord(AREA_CENTERS[area].lat, AREA_CENTERS[area].lng, 500);
    const matched = i >= 75 ? Math.random() > 0.3 : false; // Surulere tiling never matched

    await knex('demand_signals').insert({
      trade_category: trade,
      area: area,
      location_lat: coords.lat,
      location_lng: coords.lng,
      request_type: pickRandom(['job_request', 'whatsapp_inquiry', 'ussd_inquiry']),
      amount: Math.round(5000 + Math.random() * 45000),
      matched: matched,
      payment_channel: matched ? pickRandom(channels) : null,
      recorded_at: dateBetweenDaysAgo(90, 0)
    });
    demandCount++;
  }
  console.log(`  ✓ ${demandCount} demand signals inserted`);

  // === 10. Insert Apprenticeships ===
  for (const a of apprenticeships) {
    const startedAt = new Date(Date.now() - a.started_days_ago * 24 * 60 * 60 * 1000);
    await knex('apprenticeships').insert({
      master_worker_id: workerIds[a.master_worker_index],
      apprentice_id: seekerIds[a.seeker_index],
      trade: a.trade,
      duration_weeks: a.duration_weeks,
      weekly_stipend: a.weekly_stipend,
      status: a.status,
      milestones_completed: a.milestones_completed,
      total_milestones: a.total_milestones,
      started_at: startedAt
    });
  }
  console.log('  ✓ 3 apprenticeships inserted');

  console.log('\n🎉 Seed complete!');
  console.log('   Demo highlights:');
  console.log('   • Surulere has 75 tiling requests with NO tilers — visible gap');
  console.log('   • Emeka (plumber, trust 0.76) — ideal for buyer matching demo');
  console.log('   • Mama Ngozi (SabiScore 43) — "7 weeks to loan" story');
  console.log('   • Heat map clusters around 3rd Mainland Bridge axis');
}

seed()
  .then(() => process.exit(0))
  .catch((err) => {
    console.error('Seed failed:', err);
    process.exit(1);
  });
```

- [ ] **Step 2: Add seed script to package.json**

In `backend/package.json`, add to scripts:

```json
{
  "scripts": {
    "seed": "node seed.js",
    "migrate": "knex migrate:latest --knexfile src/database/knexfile.js",
    "start": "node src/index.js"
  }
}
```

- [ ] **Step 3: Verify seed script parses without syntax errors**

Run: `cd backend && node -c seed.js`

Expected: No errors (syntax check only, won't execute DB calls)

- [ ] **Step 4: Commit**

```bash
git add backend/seed.js backend/seed-data/
git commit -m "feat: add seed script with 3 months of demo data (20 workers, 50 jobs, 500 demand signals)"
```

---

### Task 5: Seed Script in Docker Startup

**Files:**
- Modify: `docker-compose.yml` (backend command)

- [ ] **Step 1: Update backend service command in docker-compose.yml**

The backend service command should run migrations then seed then start:

```yaml
backend:
  build: ./backend
  ports:
    - "3000:3000"
  environment:
    - NODE_ENV=development
  env_file:
    - .env
  depends_on:
    postgres:
      condition: service_healthy
    redis:
      condition: service_healthy
  command: sh -c "npm run migrate && npm run seed && npm run start"
```

- [ ] **Step 2: Commit**

```bash
git add docker-compose.yml
git commit -m "feat: run migrations + seed on backend startup"
```
